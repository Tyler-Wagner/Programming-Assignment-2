to install everything needed make sure you run the below code in the folder

python -m venv env 

This will create a virtual environment for python, you should see an (env) in the terminal

pip install -r requirements.txt --upgrade

this will ensure that you have all the necessary libraries for the code to run